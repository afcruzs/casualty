package causalityappproject

/*
 * Here should go all the connections between the view and the
 * logic, the model, ALWAYS do it by calling the Controller,
 * Before create another controller think if its necessary.
 * 
 * Happy Coding.
 * 
 * Felipe.
 */
class MainController {
	
    def index() { render "Welcome to causality App."}
}
